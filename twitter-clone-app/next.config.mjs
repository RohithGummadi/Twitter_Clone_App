/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["avatars.githubusercontent.com", "res.cloudinary.com", "cloudflare-ipfs.com", "errekc7uv4bopsbz.public.blob.vercel-storage.com"]
    }
};

export default nextConfig;
